[tool.pyproject]
name = "idea-luminate"
version = "0.1.0"
description = "Idea validation tool for indie hackers"
readme = "README.md"

[tool.setuptools]
packages = ["idea_luminate"]

[tool.pytest.ini_options]
testpaths = ["tests"]
